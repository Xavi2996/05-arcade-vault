# Resend (envío de correo del formulario de contacto en /acerca-de)
# Genera tu API key en https://resend.com y pégala aquí en tu .env.local (no versionado)
RESEND_API_KEY=

# Correo que recibe los mensajes del formulario de contacto
CONTACT_TO_EMAIL=xavier.cobos.29@gmail.com

# Supabase (cliente en lib/supabase.ts)
# Salen de Project Settings → API Keys en el dashboard de Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Base pública del sitio. La usa el redirectTo del login OAuth (Google/GitHub)
# para volver a /auth/callback. En desarrollo: http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
